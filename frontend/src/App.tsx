import React from 'react'
import { Button } from '@mui/material'

function App(): React.ReactElement {
  return (
    <div className="App">
      <Button variant="text">Welcom to the home page!!</Button>
    </div>
  )
}

export default App
